82. Implement the setcookie( ) function with various arguments using the PHP program.
<?php
// setcookie(name, value, expire, path, domain, secure, httponly)
setcookie("user", "Kavya", time()+3600, "/");   // expires in 1 hour
setcookie("theme", "dark", time()+86400, "/", "localhost", false, true);
if (isset($_COOKIE['user'])) {
    echo "User cookie: " . $_COOKIE['user'];
} else {
    echo "Cookie not set yet!";
}
?>
